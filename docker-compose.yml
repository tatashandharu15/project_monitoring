services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: monitoring_backend
    restart: always
    ports:
      - "9000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: monitoring_frontend
    restart: always
    ports:
      - "3333:3000"
    environment:
      - API_URL=http://backend:8000/api
    depends_on:
      - backend

  # Optional: Nginx Proxy for production-grade routing
  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
  #   depends_on:
  #     - frontend
  #     - backend
